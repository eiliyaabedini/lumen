name: lumen

x-api-env: &api-env
  ENV: ${ENV:-development}
  LOG_LEVEL: ${LOG_LEVEL:-INFO}
  TZ: ${TZ:-UTC}
  SECRET_KEY: ${SECRET_KEY}
  JWT_SECRET: ${JWT_SECRET}
  JWT_ALGORITHM: ${JWT_ALGORITHM:-HS256}
  ACCESS_TOKEN_TTL_SECONDS: ${ACCESS_TOKEN_TTL_SECONDS:-900}
  REFRESH_TOKEN_TTL_SECONDS: ${REFRESH_TOKEN_TTL_SECONDS:-1209600}
  # pydantic-settings v2 parses ``list[str]`` env vars as JSON before
  # the @field_validator(mode="before") gets a chance, so the value
  # must be a JSON array — not a comma-separated string.
  #
  # `http://web:3000` is required by the `e2e` profile: the bundle
  # served to Playwright inside the e2e container has origin
  # `http://web:3000`, and without it the api rejects every API
  # call with `400 Disallowed CORS origin` (iter 103).
  CORS_ORIGINS: ${CORS_ORIGINS:-["http://localhost:3000","http://web:3000"]}
  DATABASE_URL: ${DATABASE_URL}
  DATABASE_URL_SYNC: ${DATABASE_URL_SYNC}
  REDIS_URL: ${REDIS_URL}
  CELERY_BROKER_URL: ${CELERY_BROKER_URL}
  CELERY_RESULT_BACKEND: ${CELERY_RESULT_BACKEND}
  S3_ENDPOINT_URL: ${S3_ENDPOINT_URL}
  S3_PUBLIC_BASE_URL: ${S3_PUBLIC_BASE_URL}
  S3_REGION: ${S3_REGION}
  S3_BUCKET: ${S3_BUCKET}
  S3_ACCESS_KEY_ID: ${S3_ACCESS_KEY_ID}
  S3_SECRET_ACCESS_KEY: ${S3_SECRET_ACCESS_KEY}
  S3_FORCE_PATH_STYLE: ${S3_FORCE_PATH_STYLE}
  SMTP_HOST: ${SMTP_HOST}
  SMTP_PORT: ${SMTP_PORT}
  SMTP_FROM: ${SMTP_FROM}
  # Master switch for outbound email. ON in dev (the local `mail` MailHog
  # container is always up); mirrored here so EMAIL_ENABLED=false is
  # exercisable in dev/e2e exactly as in prod. Same pass-through lesson as
  # FEATURE_BYOK_ENABLED below.
  EMAIL_ENABLED: ${EMAIL_ENABLED:-true}
  SENTRY_DSN: ${SENTRY_DSN:-}
  OTEL_EXPORTER_OTLP_ENDPOINT: ${OTEL_EXPORTER_OTLP_ENDPOINT:-}
  PROMETHEUS_ENABLED: ${PROMETHEUS_ENABLED:-true}
  SEED_DEMO: ${SEED_DEMO:-true}
  # Runtime feature flags — mirror docker-compose.prod.yml so the
  # streaming-tutor path is exercisable in dev/e2e exactly as in prod.
  # It was the *absence* of this pass-through that let the prod-only
  # asyncpg/prefork event-loop crash ship undetected (the dev stack
  # could never turn streaming on). Defaults off; flip with
  # ``FEATURE_TUTOR_STREAMING=true`` in the env.
  FEATURE_TUTOR_STREAMING: ${FEATURE_TUTOR_STREAMING:-false}
  # BYOK (S5) — flag + versioned KEK map pass-through. Same lesson as
  # FEATURE_TUTOR_STREAMING above: without these the flag can never be
  # flipped in a real environment (S5 Gate-C finding). With the map left
  # at the JSON-empty default, dev derives a clearly-dev-only KEK from
  # SECRET_KEY; production REFUSES an empty/derived KEK at boot
  # (prod_guards + secrets_crypto).
  FEATURE_BYOK_ENABLED: ${FEATURE_BYOK_ENABLED:-false}
  BYOK_MASTER_KEYS: ${BYOK_MASTER_KEYS:-{}}
  BYOK_MASTER_KEY_VERSION: ${BYOK_MASTER_KEY_VERSION:-1}
  BYOK_ALLOW_DERIVED_KEK: ${BYOK_ALLOW_DERIVED_KEK:-false}
  # Optional server-owned AI Pass OAuth connection. Client id is injected at
  # runtime and is never compiled into the Next.js/browser bundle.
  FEATURE_AIPASS_OAUTH_ENABLED: ${FEATURE_AIPASS_OAUTH_ENABLED:-false}
  AIPASS_OAUTH_CLIENT_ID: ${AIPASS_OAUTH_CLIENT_ID:-}
  AIPASS_OAUTH_REDIRECT_URI: ${AIPASS_OAUTH_REDIRECT_URI:-http://localhost:8000/api/v1/aipass/oauth/callback}
  # S2 sharing axis (R-S8' step-4): OFF until the operator flips it.
  # Same pass-through lesson as FEATURE_BYOK_ENABLED above (S2 Gate-C).
  FEATURE_PRIVATE_PUBLISH_ENABLED: ${FEATURE_PRIVATE_PUBLISH_ENABLED:-false}
  # S4 clone/remix master gate (ADR-0028): OFF until migrations 0048/0049 are
  # fleet-confirmed + the authorizer image is rolled. Same pass-through lesson.
  CLONE_ENABLED: ${CLONE_ENABLED:-false}
  # Per-IP login rate limit. Default 10 (matches Settings + prod). The
  # e2e workflow raises it: the parallel chromium+webkit Playwright
  # suite logs in many times from one runner IP and tripped the cap.
  RATE_LIMIT_AUTH_PER_MINUTE: ${RATE_LIMIT_AUTH_PER_MINUTE:-10}
  # LLM + embedding provider selection. The e2e workflow appends
  # ``LLM_PROVIDER=noop`` to the root .env to keep AI-touching paths
  # deterministic + free; without these explicit pass-throughs the
  # api / worker / beat containers would ignore the override and fall
  # back to the ``anthropic`` default (failing on missing API key).
  # Same posture for ``EMBEDDING_PROVIDER`` so the local CPU embedder
  # stays selectable in CI. Other LLM knobs (model, api base, keys)
  # come along for the ride so an operator who flips to Groq /
  # Anthropic in .env doesn't have to also edit this anchor.
  LLM_PROVIDER: ${LLM_PROVIDER:-noop}
  LLM_MODEL: ${LLM_MODEL:-}
  LLM_MAX_TOKENS: ${LLM_MAX_TOKENS:-1024}
  EMBEDDING_PROVIDER: ${EMBEDDING_PROVIDER:-local}
  EMBEDDING_MODEL_LOCAL: ${EMBEDDING_MODEL_LOCAL:-sentence-transformers/all-MiniLM-L6-v2}
  EMBEDDING_MODEL_OPENAI: ${EMBEDDING_MODEL_OPENAI:-text-embedding-3-small}
  # Optional embedding-specific endpoint + key — lets the embedding
  # call hit a different OpenAI-compatible API (e.g. Cloudflare Workers
  # AI at /accounts/<id>/ai/v1) than the LLM (e.g. Groq on its own
  # /openai/v1). Fall back to the shared OPENAI_API_* below if unset.
  EMBEDDING_OPENAI_API_KEY: ${EMBEDDING_OPENAI_API_KEY:-}
  EMBEDDING_OPENAI_API_BASE: ${EMBEDDING_OPENAI_API_BASE:-}
  OPENAI_API_KEY: ${OPENAI_API_KEY:-}
  OPENAI_API_BASE: ${OPENAI_API_BASE:-https://api.openai.com/v1}
  ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
  ANTHROPIC_API_BASE: ${ANTHROPIC_API_BASE:-}
  NOTION_TOKEN: ${NOTION_TOKEN:-}

x-healthcheck-defaults: &healthcheck-defaults
  interval: 10s
  timeout: 5s
  retries: 10
  start_period: 15s

services:
  db:
    # pgvector/pgvector ships the upstream Postgres image with the
    # ``vector`` extension pre-installed — required by Phase E0's
    # embedding pipeline. ``pgvector/pgvector:pg17`` tracks the same
    # 17.x line as the prior ``postgres:17-alpine`` so existing dev
    # volumes upgrade in place (debian-based vs alpine, no data
    # format change).
    image: pgvector/pgvector:pg17
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-lumen}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-lumen}
      POSTGRES_DB: ${POSTGRES_DB:-lumen}
    volumes:
      - db-data:/var/lib/postgresql/data
      - ./infra/postgres/init:/docker-entrypoint-initdb.d:ro
    healthcheck:
      <<: *healthcheck-defaults
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
    ports:
      - "5432:5432"

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    command: ["redis-server", "--appendonly", "yes", "--save", "60", "1000"]
    volumes:
      - redis-data:/data
    healthcheck:
      <<: *healthcheck-defaults
      test: ["CMD", "redis-cli", "ping"]
    ports:
      - "6379:6379"

  s3:
    image: minio/minio:RELEASE.2025-01-20T14-49-07Z
    restart: unless-stopped
    command: ["server", "/data", "--console-address", ":9001"]
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:-lumen}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:-lumen-secret}
    volumes:
      - s3-data:/data
    healthcheck:
      <<: *healthcheck-defaults
      # Iter 112: MinIO needs ~10-20s on first boot before it binds
      # port 9000; the default 15s start_period sometimes trips
      # `dependency failed to start` for s3-init / api during a
      # cold `docker compose up`. Bump it so the first-boot race
      # stops being flaky.
      start_period: 30s
      test: ["CMD", "curl", "-fsS", "http://127.0.0.1:9000/minio/health/live"]
    ports:
      - "9000:9000"
      - "9001:9001"

  s3-init:
    image: minio/mc:RELEASE.2025-01-17T23-25-50Z
    depends_on:
      s3:
        condition: service_healthy
    entrypoint: ["/bin/sh", "-c"]
    command:
      - |
        set -e
        mc alias set local http://s3:9000 "$$MINIO_ROOT_USER" "$$MINIO_ROOT_PASSWORD"
        mc mb -p local/$$S3_BUCKET || true
        mc anonymous set download local/$$S3_BUCKET
        mc admin user svcacct add local "$$MINIO_ROOT_USER" --access-key "$$S3_ACCESS_KEY_ID" --secret-key "$$S3_SECRET_ACCESS_KEY" || true
        echo "MinIO bootstrap complete"
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:-lumen}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:-lumen-secret}
      S3_BUCKET: ${S3_BUCKET:-lumen-assets}
      S3_ACCESS_KEY_ID: ${S3_ACCESS_KEY_ID:-lumen}
      S3_SECRET_ACCESS_KEY: ${S3_SECRET_ACCESS_KEY:-lumen-secret}
    restart: "no"

  mail:
    image: axllent/mailpit:v1.20
    restart: unless-stopped
    ports:
      - "1025:1025"
      - "8025:8025"

  api:
    build:
      context: ./apps/backend
      target: dev
    image: lumen-api:dev
    restart: unless-stopped
    environment: *api-env
    volumes:
      - ./apps/backend:/app
    depends_on:
      db: { condition: service_healthy }
      redis: { condition: service_healthy }
      s3: { condition: service_healthy }
    command: ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000", "--reload", "--reload-dir", "app"]
    healthcheck:
      <<: *healthcheck-defaults
      test: ["CMD", "python", "-c", "import urllib.request,sys; sys.exit(0 if urllib.request.urlopen('http://localhost:8000/api/v1/health/live', timeout=2).status==200 else 1)"]
    ports:
      - "8000:8000"

  worker:
    build:
      context: ./apps/backend
      target: dev
    image: lumen-api:dev
    restart: unless-stopped
    environment: *api-env
    volumes:
      - ./apps/backend:/app
    depends_on:
      api: { condition: service_started }
      redis: { condition: service_healthy }
    command: ["celery", "-A", "app.workers.celery_app.celery", "worker", "--loglevel=INFO", "--concurrency=2"]

  beat:
    build:
      context: ./apps/backend
      target: dev
    image: lumen-api:dev
    restart: unless-stopped
    environment: *api-env
    volumes:
      - ./apps/backend:/app
    depends_on:
      api: { condition: service_started }
      redis: { condition: service_healthy }
    command: ["celery", "-A", "app.workers.celery_app.celery", "beat", "--loglevel=INFO"]

  web:
    build:
      context: ./apps/frontend
      target: dev
    image: lumen-web:dev
    restart: unless-stopped
    environment:
      NODE_ENV: development
      NEXT_PUBLIC_API_BASE_URL: ${NEXT_PUBLIC_API_BASE_URL:-http://localhost:8000}
      NEXT_PUBLIC_WS_BASE_URL: ${NEXT_PUBLIC_WS_BASE_URL:-ws://localhost:8000}
      API_INTERNAL_BASE_URL: http://api:8000
      PORT: "3000"
    volumes:
      - ./apps/frontend:/app
      - web-node-modules:/app/node_modules
      - web-next-cache:/app/.next
      # Read-only mount of the compose file so the iter 99
      # vitest regression (tests/e2e-image-pin.test.ts) can
      # confirm the Playwright image tag stays in lock-step with
      # @playwright/test in package.json. Reached via the
      # /repo/docker-compose.yml symlink resolution in the test.
      - ./docker-compose.yml:/repo/docker-compose.yml:ro
      # Same mount pattern for the Makefile so the regression
      # guard in tests/makefile-pnpm-invocation.test.ts can pin
      # the `test.web` target to `pnpm exec vitest run` (the
      # broken `pnpm test --run` form errors on pnpm 9.15.0).
      - ./Makefile:/repo/Makefile:ro
      # And the workflows directory so tests/ci-workflow-shape.test.ts
      # can pin the deploy job's `needs:` list and the
      # `environment: production` gate against accidental removal.
      - ./.github/workflows:/repo/.github/workflows:ro
    depends_on:
      api: { condition: service_started }
    command: ["pnpm", "dev"]
    ports:
      - "3000:3000"

  # Dedicated Playwright runner. The `web` image is alpine-based
  # (musl libc) and Playwright doesn't ship browser binaries for
  # musl — `pnpm exec playwright install` inside `web` either
  # fails or pulls glibc binaries that segfault. The custom
  # Dockerfile.e2e extends mcr.microsoft.com/playwright (chromium
  # / firefox / webkit pre-built against glibc) and bakes
  # `pnpm install` into an image layer so node_modules doesn't
  # have to be re-built into a Docker volume on every run —
  # critical on Windows Docker Desktop where the pnpm symlink
  # fan-out into a named volume crawls (~10 packages/min). The
  # anonymous `/work/node_modules` volume keeps the bind-mount
  # of `./apps/frontend:/work` from shadowing the baked install.
  #
  # Profile-gated so `docker compose up` doesn't start it:
  #     docker compose --profile e2e run --rm e2e
  #     make test.e2e
  #
  # The image's Playwright version is enforced by
  # tests/e2e-image-pin.test.ts to stay pinned to
  # @playwright/test in package.json.
  e2e:
    profiles: ["e2e"]
    build:
      context: ./apps/frontend
      dockerfile: Dockerfile.e2e
    image: lumen-e2e:dev
    environment:
      E2E_BASE_URL: ${E2E_BASE_URL:-http://web:3000}
      # Loop 8 — auth.setup.ts logs in via the FastAPI endpoint
      # directly instead of clicking the /login form, which used to
      # race the dev-mode JIT compile non-deterministically and was
      # the root cause of three deferred light-mode auth-gated VR
      # baselines (dashboard-light, admin-light, studio-light).
      # localhost from inside the e2e container is the e2e
      # container itself, not the api container — `api:8000` is the
      # docker-network name.
      E2E_API_BASE_URL: ${E2E_API_BASE_URL:-http://api:8000}
    volumes:
      - ./apps/frontend:/work
      # Anonymous volume preserves the image's baked
      # node_modules from the bind-mount above.
      - /work/node_modules
    depends_on:
      web: { condition: service_started }
      api: { condition: service_started }

volumes:
  db-data:
  redis-data:
  s3-data:
  web-node-modules:
  web-next-cache:

networks:
  default:
    name: lumen-dev
